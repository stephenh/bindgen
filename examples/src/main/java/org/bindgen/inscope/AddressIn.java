package org.bindgen.inscope;

import org.bindgen.Bindable;

/**
 * A bean that is in bindgen's scope but not explicitly
 * annotated as {@link Bindable}.
 */
public class AddressIn {
	public String city;
}
